module.exports = parseAddress

function parseAddress (walletAddressInfo) {
  let address = {
    street: '',
    city: ''
  }
  // TO DO: parse the address and get the street and city

  return address
}
